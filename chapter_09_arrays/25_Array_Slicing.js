let arr = [10, 20, 30, 40, 50];
console.log(arr.slice(1,4)); // will take elements from index 1 to 3

console.log(arr.slice(2)); // will take all elements from index 2

console.log(arr.slice(-2)); // will work as above for negative index also
