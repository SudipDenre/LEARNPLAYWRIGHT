const calculator = {
    value:0,
    add(n){

    },
    subtract(n){

    }
};

// Objects can contain functions also inside it.

// Basic Destructuring
const user = {name1:"Sudip", age:25, city:"NYC"};

const {name1, age} = user;

console.log(name1);
console.log(age);

