const user = {
    name: "Sudip",
    age: 25,
    city: "Kolkata"
};

// Extract name and age using object destructuring.

const {name, age} = user;
console.log(name);
console.log(age);

const colors = ["Red", "Green", "Blue"];

let firstColor = colors[0];
let secColor = colors[1];
console.log(firstColor);
console.log(secColor);

